import { Component, OnDestroy, OnInit } from '@angular/core';import { ActivatedRoute, Router } from '@angular/router';import { Subject, takeUntil } from 'rxjs';import { Image } from '~/dashboard/models/image.model';import { AlbumsService } from '~/dashboard/services/albums.service';@Component({  selector: 'app-album',  templateUrl: './album.component.html',  styleUrls: ['./album.component.scss']})export class AlbumComponent implements OnInit, OnDestroy {  private componentDestroyed$ = new Subject();  public gallery: Image[];  constructor(    private activatedRoute: ActivatedRoute,    private albumsService: AlbumsService,    private router: Router  ) {}  public ngOnInit() {    this.activatedRoute.params      .pipe(takeUntil(this.componentDestroyed$))      .subscribe((params) => this.initGallery(+params.id));  }  public ngOnDestroy() {    this.componentDestroyed$.unsubscribe();  }  private initGallery(id: number) {    if (this.albumsService.albumsList.length === 0) {      this.router.navigateByUrl('/dashboard');    }    this.gallery = this.albumsService.albumsList.find((item) =>      item.id === id    ).images;  }}